# Production API base for the live lead API (Cloud Run, project trendpulse-ai-8fc15).
# Used by `npm run build`; the hosted frontend calls this directly (CORS is open).
# Local dev ignores this and proxies /api to the local server via vite.config.js.
VITE_API_BASE=https://trendpulse-lead-api-28815536054.us-central1.run.app
